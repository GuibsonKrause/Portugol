algoritmo "semnome"

var
 n  : real
 tot, i : inteiro
inicio
    tot<-0
     para i de 1 ate 80 faca
     leia (n)
     se (n >= 10) e (n <= 150) entao
     tot <- tot + 1
     fimse
     fimpara
     escreva (tot)
fimalgoritmo
