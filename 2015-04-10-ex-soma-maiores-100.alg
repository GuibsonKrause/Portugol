algoritmo "soma maiores que 100"
var
   n, i, soma : inteiro
   
inicio
      soma <- 0
      para i de 1 ate 50 faca
           leia(n)
           se (n > 100) entao
              soma <- soma + n
           fimse
      fimpara
fimalgoritmo
